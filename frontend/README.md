# To-Do Frontend

This folder contains the React and Vite frontend for the full-stack To-Do project.

Use the complete setup instructions in the root [`README.md`](../README.md).

Local environment variable:

```env
VITE_API_URL=http://localhost:5000/api/tasks
```
